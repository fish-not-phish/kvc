# kvc smoketest .env — placeholders here are resolved by kvc and passed to
# docker compose via subprocess env for ${VAR} interpolation in compose.yml.
# Copy this to .env and replace the placeholder with a real path.
#
# Example Vault secret to create:
#   vault kv put kv/api key="abc123"

API_KEY=@@kv/api#key@@

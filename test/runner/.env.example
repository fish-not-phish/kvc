# test/runner/.env — kvc resolves the placeholder below and passes the value
# to docker compose via subprocess env, where ${RUNNER_API_KEY} in the
# compose file picks it up.
RUNNER_API_KEY=@@kv/runner#api_key@@
